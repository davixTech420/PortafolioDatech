import { useState } from "react";
import { ThemedView,ThemedText } from "@components/ThemedView";

export const Footer = () => {
    return(
        <ThemedView>
            <ThemedText>Como es</ThemedText>
        </ThemedView>
    );
}
